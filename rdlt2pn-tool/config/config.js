/*
A singleton-style module that holds configuration settings. 
*/

// config/config.js
const config = {
    loopLimit: 10,
    timeConstraint: 1000,
    // Add additional configuration settings as needed
  };
  
  export default config;
  